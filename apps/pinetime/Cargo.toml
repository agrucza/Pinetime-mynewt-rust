[package]
name = "pinetime"
version = "0.1.0"
authors = ["agrucza <a.grucza@gmx.net>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
panic-halt = "0.2.0"

[lib]
crate-type = ["staticlib"]
